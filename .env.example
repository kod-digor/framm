# Variables obligatoires
SCW_ACCESS_KEY=
SCW_SECRET_KEY=
SCW_PROJECT_ID=
ADMIN_PASSWORD=
BUREAU_ADMIN_EMAIL=

# Optionnel — clé SSH (auto-détectée depuis ~/.ssh/ si absente)
# SSH_PUBLIC_KEY=ssh-ed25519 AAAA...

# Optionnel — DNS (auto-détecté au bootstrap)
# DNS_ENABLED=true

# Optionnel — domaine app.bzh
# APP_BZH_ENABLED=false
