services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: framm
      POSTGRES_PASSWORD: framm
      POSTGRES_DB: framm
    ports:
      - "5432:5432"
    volumes:
      - dev_postgres:/var/lib/postgresql/data

volumes:
  dev_postgres:
